import React from 'react'

function Desk({desk}) {
//  let em = user.email;
//  let eml = Object.keys(em).length;
//  let deskSpacing = Array(50).fill('\xa0').join('');

  return (
    <div>
      <h2>
        {desk}
      </h2>
    </div>
  )
}

export default Desk